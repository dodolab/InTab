<UserControl x:Class="InteractiveTable.GUI.Other.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch"
             Height="23">
    <Menu Name="menu" DataContext="{Binding}">
        <MenuItem Name="fileItem" Header="File">
            <MenuItem Name="systemLoad" Header="Load system" />
            <MenuItem Name="systemSave" Header="Save system" />
            <MenuItem Name="endApp" Header="Exit" />
        </MenuItem>
        <MenuItem Name="tableItem" Header="Table">
            <MenuItem Name="openGLItem" Header="OpenGL window"/>
        </MenuItem>
        <MenuItem Name="settingItem" Header="Settings">
            <MenuItem Name="userSettingsItem" Header="User settings" />
            <MenuItem Name="tableCalibrationItem" Header="Table calibration"/>
            <MenuItem Name="contourSubItem" Header="Contour settings" />
        </MenuItem>
    </Menu>
</UserControl>
